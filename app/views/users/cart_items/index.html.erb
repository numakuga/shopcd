<h2 class="font-weight-bold border-bottom pb-2 mb-5">
  <%= @page_title = "カート" %>
</h2>

<% if @cart_items.present? %>
<p>現在カートには、こちらの商品が入っております。</p>
<h5 class="card-header row sticky-top border p-4 font-weight-bold d-flex justify-content-around">
  <sapn class="col-md-6 text-center">商品</sapn>
  <span class="col-md-1">数量</span>
  <span class="col-md-2">金額</span>
  <span class="col-md-2 mr-5">在庫数</span>
</h5>

  <% @cart_items.each do |cart_item| %>
    <div class="card row bl-blue mt-4">
      <div class="card-body d-flex justify-content-between">

        <div class="col-md-3">
          <div class="card">
            <%= link_to item_path(cart_item.item) do %>
              <%= image_tag "logo", size: "250x250" %>
            <% end %>
          </div>
        </div>

        <div class="align-self-center">
          <%= link_to item_path(cart_item.item) do %>
            <h3 class="card-title"><%= cart_item.item.title %></h3>
          <% end %>
          <p class="card-text"><%= cart_item.item.artist.name %></p>
        </div>
        <p class="card-text align-self-center"><%= cart_item.piece %></p>
        <!-- メソッドモデルに移動したい -->
        <p class="card-text align-self-center"><%= (cart_item.item.price * 1.10).round.to_s(:delimited) %>円(税込)</p>
        <p class="card-text align-self-center">在庫数：<%= cart_item.item.stock %>点</p>
        <%= link_to "削除", user_cart_item_path(current_user, cart_item), method: :delete, class:"btn btn-secondary align-self-center" %>
      </div>
    </div>
  <% end %>
  <div class="card row mt-4">
    <div class="card-body d-flex justify-content-end">
      <h5 class="card-text mr-5">計<%= @cart_items.pluck(:piece).sum %>点</h5>
      <!-- メソッドモデルに移動したい -->
      <h4 class="card-text">合計金額<%= @cart_items.map{|cart_item| (cart_item.item.price * cart_item.piece * 1.10).round}.sum.to_s(:delimited) %>円(税込)</h4>
    </div>
  </div>
<% else %>
  <h4>現在お客様のショッピングカートに商品はありません。</h4>
<% end %>


<div class="row d-flex justify-content-between">
  <%= link_to "＜　買い物を続ける", root_path, class: "btn btn-info col-md-4 mt-5" %>
  <% if @cart_items.present? %>
    <%= link_to "購入に進む　＞", user_orders_path(current_user), class: "btn btn-info col-md-4 mt-5" %>
  <% else %>
    <%= link_to "購入に進む　＞", "#", class: "btn btn-info col-md-4 mt-5 disabled" %>
  <% end %>
</div>
